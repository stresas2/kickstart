{% extends 'base.html.twig' %}

{% block title %}Symfony 3{% endblock %}

{% block body %}
    {% if user %}
        Prisjungęs: {{ user.username }}
    {% else %}
        Anonimas
    {% endif %}

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <hr/>
        <p>Email: {{ app.user.email }}</p>
    {% endif %}
{% endblock %}
